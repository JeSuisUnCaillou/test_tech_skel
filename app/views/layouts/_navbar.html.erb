<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header navbar-nav">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
			<div class="<%=set_active('home', 'index')%>">
				<%= link_to "Find.me", root_path, class:"navbar-brand" %>
			</div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
				<li class="<%=set_active('searches', 'index')%>"><%= link_to "My Searches", searches_path %></li>
      </ul>

			<%= form_for(Search.new, html: {class: "navbar-form navbar-left"}) do |f| %>
				<div class="form-group">
					<%= f.text_field(:sentence, class: "form-control", placeholder: "Search")%>
				</div>
				<%= f.submit("Search", class:"btn btn-default") %>
			<% end %>

      <ul class="nav navbar-nav navbar-right">
        <!--li><a href="#">Link</a></li-->
				<li>
					<p class="navbar-text">
					<% if user_signed_in? %>
						Signed in as <%=current_user.email %>. Not you? 
						<%= link_to "Sign out", destroy_user_session_path, :method => :delete, class: "navbar-link" %>
					<% else %>
						<%= link_to "Sign up", new_user_registration_path, class: "navbar-link"  %> or <%= link_to "sign in", new_user_session_path, class: "navbar-link"  %>
					<% end %>
					</p>
				</li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>


